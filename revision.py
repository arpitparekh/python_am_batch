# variables
# datatypes
# typecastin
# list tuple and dictionary(key value pair)
# operators
# control flow (conditional statements)
# loops (for, while)
# loop inside loop (patterns)
# functions
def sum(a,b): return a+b
print(sum(12,13))
# oppc
# class object
class Student:
  def __init__(self,name,age):
    self.name = name
    self.age = age
  def display(self):
    print(self.name,self.age)

s = Student("suimt",23)
s.display()

# constrcutor
# inheritance (types of inheritance)
# polymorphism
# modules
import operator   # system modules
print(operator.mul(12,13))

import intro
# file hanlding
# threading
# exception handling(try, except, finally)
# regex
# dattime  # date formatting
# math
import re  # pending

import sys
print(sys.version)
print(sys.platform)
import os

# core python
# mysql
# mongodb # nosql database

# advance python
# database handling (local database, remote database)(mysql,mongodb)
# api (rest api) // fast api // exios api // crud operation
# desktop application // tkinter (windows,linux,mac) // pyQT
# web application // flask // django (web appilcation)


# 2 types of database
# 1. local database (database server that run on my local machine)
# 2. remote database (database server that run on rweb servers)

# local database

# mysql(database => tables) // mongodb(database => collections => documents)
# create table
# insert data into table
# read table
# update table
# delete table

# mysql
